executor
	- pipe()	error처리 완
	- fork()	error처리 완

child_process
	set_redirection
		set_file_redirection
			- open()	더 생각..
			- dup2()	더 생각..
		- dup2()
	get_valid_cmd_path
		- ft_split()?	free완
		double_strjoin
			- ft_strjoin()?		free완
		- cmd_path(리턴값) 		free완
		- path free			완
	list_to_arr
		- arr(리턴값) free		완
		- malloc NULL check		?
	- execve()

parent_process
	- dup2()
	- close()?

각 에러 처리(perror) 후, 진행 흐름을 어떻게 할지..
perror 한번만 뜨게..